[project]
name = "axiom"
version = "0.1.0"
description = "Autonomous AI Research System"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]